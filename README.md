# Matrix-set
- some files have been slightly changed for security.
- formating might be broken.
- and im aware ofhow messy this code is
- going to any of the domains listed will be blocked, this is because of cloudflare (and yes it works fine with matrix clients)
- matrix-turnify runs in a docker with qemu emulation (aarch64)
